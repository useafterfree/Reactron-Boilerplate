let Ipc = require('../shared/ipc');
let path = require('path');
let fs = require('fs');
let async = require('async');
let base64 = require('file-base64');
var spawn = require('child_process').spawn

const load = (electron, app, browserWindow) => {};

exports.load = load;
